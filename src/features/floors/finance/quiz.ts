import { QuizDefinition } from '../../../config/quiz/types';

/**
 * Quizzes for the Finance Team floor (left room of floor 3 / Business).
 *
 * Empty placeholder — ready for the Finance floor owner to add content.
 */
export const QUIZ_FINANCE: Record<string, QuizDefinition> = {};
