import { QuizDefinition } from './types';

/**
 * Quizzes for the Product Leadership room (floor 3) and the
 * Products hall / product rooms (floor 5).
 *
 * Empty placeholder — ready for the Product floor owner to add content.
 */
export const QUIZ_PRODUCT: Record<string, QuizDefinition> = {};
