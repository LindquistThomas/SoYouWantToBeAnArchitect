import { QuizDefinition } from './types';

/**
 * Quizzes for the Executive Suite floor (penthouse).
 *
 * Empty placeholder — ready for the Executive floor owner to add content.
 */
export const QUIZ_EXEC: Record<string, QuizDefinition> = {};
